# Demo screenshots

Place demo evidence here (e.g. a screenshot of the Extension Development Host showing the
**Local Reviews** view and inline comment threads rendered from the bundled fixture).

See `../../DEMO.md` for the full, repeatable demo instructions (Option A: F5 dev host;
Option B: sideload the `.vsix`; Option C: VM sideload via hyperloop).
